imports:
    - { resource: parameters.yml }
    - { resource: security.yml }
    - { resource: @UserBundle/Resources/config/services.yml }
#    - { resource: @MessageBundle/Resources/config/services.yml }


    
    
framework:
    #esi:             ~
    translator:      { fallback:  %fallback_locale%  }
    default_locale: %locale%
    secret:          "%secret%"
    router:
        resource: "%kernel.root_dir%/config/routing.yml"
        strict_requirements: ~
    form:            ~
    csrf_protection: ~
    validation:      { enable_annotations: true }
    templating:
        engines: ['twig']
        #assets_version: SomeVersionScheme
    default_locale:  "%locale%"
    trusted_hosts:   ~
    trusted_proxies: ~
    session:
        # handler_id set to null will use default session handler from php.ini
        handler_id:  ~
    fragments:       ~
    http_method_override: true

# Twig Configuration
twig:
    debug:            "%kernel.debug%"
    strict_variables: "%kernel.debug%"
#    
#twig.extension.intl:
#    class: Twig_Extensions_Extension_Intl
#    tags:
#        - { name: twig.extension }
#        
        
# Assetic Configuration
assetic:
    debug:          "%kernel.debug%"
    use_controller: false
    #bundles:        [UserBundle,PagesBundle]
    #java: /usr/bin/java
    filters:
        cssrewrite: ~
        #closure:
        #    jar: "%kernel.root_dir%/Resources/java/compiler.jar"
        #yui_css:
        #    jar: "%kernel.root_dir%/Resources/java/yuicompressor-2.4.7.jar"

# Doctrine Configuration
doctrine:
    dbal:
        driver:   "%database_driver%"
        host:     "%database_host%"
        port:     "%database_port%"
        dbname:   "%database_name%"
        user:     "%database_user%"
        password: "%database_password%"
        charset:  UTF8
        # if using pdo_sqlite as your database driver, add the path in parameters.yml
        # e.g. database_path: "%kernel.root_dir%/data/data.db3"
        # path:     "%database_path%"

    orm:
        auto_generate_proxy_classes: "%kernel.debug%"
        auto_mapping: true

# Swiftmailer Configuration
swiftmailer:
    transport: "%mailer_transport%"
    host:      "%mailer_host%"
    username:  "%mailer_user%"
    password:  "%mailer_password%"
    spool:     { type: memory }
    
# FOSUserBundle Configuration

fos_user:
    db_driver: orm # other valid values are 'mongodb', 'couchdb' and 'propel'
    firewall_name: main
    user_class: ICGM2\UserBundle\Entity\User    
    registration:
        form:
            type: icgm2_userbundle_user
    resetting:
      email:
         from_email:
            address: noreply@icgm.com
            sender_name: ICGM Inc.
            
    profile:
        form:
            type: icgm2_edit_profile
            
#fos_message:
#    db_driver: orm
#    thread_class: ICGM2\MessageBundle\Entity\Thread
#    message_class: ICGM2\MessageBundle\Entity\Message
#    new_thread_form:
#        type: icgm2_new_thread.new_thread_form.type
#        model: ICGM2\MessageBundle\FormModel\NewThreadMessage
        